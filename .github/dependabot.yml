# Keep SHA-pinned GitHub Actions and Cargo dependencies current (weekly PRs).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: cargo
    directory: /broker
    schedule:
      interval: weekly
